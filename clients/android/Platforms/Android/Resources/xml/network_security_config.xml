<?xml version="1.0" encoding="utf-8"?>
<!--
    Cleartext (HTTP) is permitted ONLY for the emulator host alias and localhost,
    so the default http://10.0.2.2:3978 target works while every other host still
    requires TLS. Real devices should use a Dev Tunnel https URL (allowed by the
    base-config below). To allow another cleartext host, add a <domain> entry.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
